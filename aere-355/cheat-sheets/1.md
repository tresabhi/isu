# AERE 355 Cheat Sheet 1

## Trivial Preliminaries

Pure:

$$
\theta < 15\degree \implies \sin \theta \approx \theta
$$

$$
\delta(\theta) = \frac{\theta - \sin \theta}{\sin \theta}
$$

Bernoulli:

$$
P_1 + \frac{1}{2} \rho V_1^2 + \rho g z_1 = P_2 + \frac{1}{2} \rho V_2^2 + \rho g z_2
$$

Compressible fork:

$$
P = c \rho^\gamma
$$

$$
\gamma_{air} \approx 1.4
$$

$$
\frac{1}{2}V^2 + \frac{P \gamma}{\rho(\gamma - 1)} + \cancel{g z}^\text{often ignored} = C
$$

Atmosphere:

$$
dP = \rho g dh
$$

$$
g = g_0 \left( \frac{R_o}{R_o + h_G} \right)^2
$$

$$
h_G = \left( \frac{R_o}{R_o - h} \right) h
$$

$$
P = \rho R T
$$

$$
T = T_1 + \lambda (h - h_1)
$$

$$
\frac{P}{P_1}

= \left( \frac{T}{T_1} \right)^\frac{-g_o}{R \lambda}

= \frac{\rho T}{\rho_1 T_1}

= e^{\frac{-g_o}{R T_1} (h - h_1)}
$$

$$
\frac{\rho}{\rho_1}

= \left( \frac{T}{T_1} \right) ^ {-1 - \frac{g_o}{R \lambda}}

= \frac{\rho}{\rho_1}

= e^{\frac{-g_o}{R T_1} (h - h_1)}
$$

## Novel Preliminaries

Coordinate system:

![](https://i.imgur.com/jp8bvKRm.png) ![](https://i.imgur.com/4SgQzJIm.png) ![](https://i.imgur.com/p6tGiIum.png) ![](https://i.imgur.com/1b0PdSbm.png) ![](https://i.imgur.com/NqRoIvam.png)

$$
V_y = v
$$

$$
\beta = \arcsin \frac{v}{V}
$$

$$
V = \sqrt{u^2 + v^2 + w^2}
$$

$$
\alpha = \arctan w/u = \arccos u/V_p = \arcsin w/V_p \approx w/u
$$

$$
\beta = \arcsin v/V = \arccos V_p/V = \arctan v/V_p \approx v/u
$$

$$
V_p = u / \cos \alpha
$$

$$
\beta = \arctan \frac{v \cos \alpha}{u}
$$

Forces:

$$
X = C_x Q S \qquad \text{Axial Force}
$$

$$
Y = C_y Q S \qquad \text{Side Force}
$$

$$
Z = C_z Q S \qquad \text{Normal Force}
$$

## Aerodynamic Preliminaries

$$
Q = \frac{1}{2} \rho V^2
$$

$$
C_L = C_{L_0} + C_{L_\alpha} \alpha
$$

$$
C_D = C_{D_{min}} + K C_L^2
$$

## Aircraft Dynamics

![](https://i.imgur.com/QSMEKkrm.png)

Normalization:

$$
h = \frac{x_cg}{\bar{c}}
$$

$$
h_{ac} = \frac{x_{ac}}{\bar{c}}
$$

Moments:

$$
\begin{align*}
  C_m &= C_{m_{ac}} + C_L (h - h_{ac}) \\
  &= C_{m_{ac}} + (C_{L_0} + C_{L_\alpha} \alpha) (h - h_{ac}) \\
  &= [C_{m_{ac}} + C_{L_0} (h - h_{ac})] + [C_{L_\alpha} (h - h_{ac})] \alpha \\
  &= C_{m_0} + C_{m_\alpha} \alpha
\end{align*}
$$

$$
C_{m_0} = C_{m_{ac}} + C_{L_0} (h - h_{ac})
$$

$$
C_{m_\alpha} = \frac{d C_m}{d \alpha} = C_{L_\alpha} (h - h_{ac})
$$

Passive stability:

$$
C_{m_\alpha} < 0 \cup C_{m_0} > 0
$$
