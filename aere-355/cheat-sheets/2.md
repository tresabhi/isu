# AERE 355 Cheat Sheet 2

## Trivial Preliminaries

Pure:

$$
\theta < 15\degree \implies \sin \theta \approx \theta
$$

$$
\delta(\theta) = \frac{\theta - \sin \theta}{\sin \theta}
$$

Bernoulli:

$$
P_1 + \frac{1}{2} \rho V_1^2 + \rho g z_1 = P_2 + \frac{1}{2} \rho V_2^2 + \rho g z_2
$$

Compressible fork:

$$
P = c \rho^\gamma
$$

$$
\gamma_{air} \approx 1.4
$$

$$
\frac{1}{2}V^2 + \frac{P \gamma}{\rho(\gamma - 1)} + \cancel{g z}^\text{often ignored} = C
$$

Atmosphere:

$$
dP = \rho g dh
$$

$$
g = g_0 \left( \frac{R_o}{R_o + h_G} \right)^2
$$

$$
h_G = \left( \frac{R_o}{R_o - h} \right) h
$$

$$
P = \rho R T
$$

$$
T = T_1 + \lambda (h - h_1)
$$

$$
\frac{P}{P_1}

= \left( \frac{T}{T_1} \right)^\frac{-g_o}{R \lambda}

= \frac{\rho T}{\rho_1 T_1}

= e^{\frac{-g_o}{R T_1} (h - h_1)}
$$

$$
\frac{\rho}{\rho_1}

= \left( \frac{T}{T_1} \right) ^ {-1 - \frac{g_o}{R \lambda}}

= \frac{\rho}{\rho_1}

= e^{\frac{-g_o}{R T_1} (h - h_1)}
$$

## Aerospace Preliminaries

$$
Q = \frac{1}{2} \rho V^2
$$

$$
C_L = C_{L_0} + C_{L_\alpha} \alpha
$$

$$
C_D = C_{D_{min}} + K C_L^2
$$

$$
\begin{align*}
  C_{L_\alpha}
  &= \frac{C_{l_\alpha}}{1 + \frac{C_{l_\alpha}}{\pi AR}} \quad \text{(for radians)} \\
  &= \frac{C_{l_\alpha}}{1 + 57.3 \frac{C_{l_\alpha}}{\pi AR}} \quad \text{(for degrees)}
\end{align*}
$$

$$
AR = \frac{b^2}{S}
$$

## Novel Preliminaries

Coordinate system:

![](https://i.imgur.com/jp8bvKRm.png) ![](https://i.imgur.com/4SgQzJIm.png)

![](https://i.imgur.com/p6tGiIum.png) ![](https://i.imgur.com/1b0PdSbm.png)

![](https://i.imgur.com/NqRoIvam.png)

$$
V_y = v
$$

$$
\beta = \arcsin \frac{v}{V}
$$

$$
V = \sqrt{u^2 + v^2 + w^2}
$$

$$
\alpha = \arctan w/u = \arccos u/V_p = \arcsin w/V_p \approx w/u
$$

$$
\beta = \arcsin v/V = \arccos V_p/V = \arctan v/V_p \approx v/u
$$

$$
V_p = u / \cos \alpha
$$

$$
\beta = \arctan \frac{v \cos \alpha}{u}
$$

Forces:

$$
\begin{align*}
  X &= C_x Q S \qquad &\text{Axial Force} \\
  Y &= C_y Q S \qquad &\text{Side Force} \\
  Z &= C_z Q S \qquad &\text{Normal Force}
\end{align*}
$$

## Aircraft Dynamics

![](https://i.imgur.com/QSMEKkrm.png) ![](https://i.imgur.com/jS9qgnMm.png) ![](https://i.imgur.com/ReeqIrQm.png) ![](https://i.imgur.com/r8LDX3Nl.png)

Lengths:

$$
h = \frac{x_cg}{\bar{c}}
$$

$$
h_{ac} = \frac{x_{ac}}{\bar{c}}
$$

$$
h_{ac_w} = h_{ac}
$$

Wing moments:

$$
\begin{align*}
  C_m
  &= C_{m_{ac}} + C_L (h - h_{ac}) \\
  &= C_{m_{ac}} + C_{m_\alpha} \alpha \\
  &= C_{m_{ac}} + (C_{L_0} + C_{L_\alpha} \alpha) (h - h_{ac}) \\
  &= [C_{m_{ac}} + C_{L_0} (h - h_{ac})] + [C_{L_\alpha} (h - h_{ac})] \alpha \\
  &= C_{m_0} + C_{m_\alpha} \alpha
\end{align*}
$$

$$
C_{m_0} = C_{m_{ac}} + C_{L_0} (h - h_{ac})
$$

$$
C_{m_\alpha} = \frac{d C_m}{d \alpha} = C_{L_\alpha} (h - h_{ac})
$$

$$
C_{m_w} = C_{m_{0_w}} + C_{m_{\alpha_w}} \alpha
$$

$$
C_{m_{0_w}} = C_{m_{ac_w}} + C_{L_{0_w}} (h - h_{ac})
$$

$$
C_{m_{\alpha_w}} = C_{L_{\alpha_w}} (h - h_{ac})
$$

$$
C_{L_w} = C_{L_{0_w}} + C_{L_{\alpha_w}} \alpha
$$

Tail moments:

$$
M_t = -L_t l_t = -C_{L_t} Q_t S_t l_t
$$

$$
\begin{align*}
  C_{m_t}
  &= C_{m_{0_t}} + C_{m_{\alpha_t}} \alpha \\
  &= \frac{M_t}{Q S \bar{c}} \\
  &= -\eta V_H C_{L_t} \\
  &= -\eta \left[ \bar{V}_H - \frac{S_t}{S} (h - h_{ac}) \right] C_{L_t}
\end{align*}
$$

$$
C_{m_{0_t}} = \eta V_H C_{L_{\alpha_t}} (\epsilon_0 + i_w - i_t)
$$

$$
C_{m_{\alpha_t}} = -\eta V_H C_{L_{\alpha_t}} (1 - \frac{d\epsilon}{d\alpha})
$$

$$
\eta = \frac{V_t^2 / 2}{V_w^2 / 2} = \frac{Q_t}{Q_w} \approx 1 \quad \text{approximation for most cases}
$$

$$
\begin{align*}
  V_H
  &= \frac{l_t S_t}{\bar{c} S} \\
  &= \left[ \frac{\bar{l}_t}{\bar{c}} - (h - h_{ac}) \right] \frac{S_t}{S} \\
  &= \frac{\bar{l}_t S_t}{\bar{c} S} - \frac{S_t}{S} (h - h_{ac}) \\
  &= \bar{V}_H - \frac{S_t}{S} (h - h_{ac})
\end{align*}
$$

$$
\frac{l_t}{\bar{c}} = h_{ac_t} - h = \frac{\bar{l}_t}{\bar{c}} - (h - h_{ac})
$$

$$
\begin{align*}
  \epsilon_0
  &= 57.3 \frac{2 C_{L_{0_w}}}{\pi AR_w} &\text{for degrees} \\
  &= \frac{2 C_{L_{0_w}}}{\pi AR_w} &\text{for radians}
\end{align*}
$$

$$
\begin{align*}
  \frac{d\epsilon}{d\alpha}
  &= 57.3 \frac{2 C_{L_{\alpha_w}}}{\pi AR_w} &\text{for degrees} \\
  &= \frac{2 C_{L_{\alpha_w}}}{\pi AR_w} &\text{for radians}
\end{align*}
$$

$$
C_{L_t} = C_{L_{\alpha_t}} \alpha_t = C_{L_{\alpha_t}} (\alpha_w - (i_w - i_t) - \epsilon)
$$

Airplane moments:

$$
\begin{align*}
  C_m
  &= C_{m_0} + C_{m_\alpha} \alpha \\
  &= (C_{m_{0_w}} + C_{m_{0_t}}) + (C_{m_{\alpha_w}} + C_{m_{\alpha_t}}) \alpha \\
  &= C_{m_w} + C_{m_t} \\
  &= C_{m_{ac_w}} + \left[ C_{L_w} + \eta \frac{S_t}{S} C_{L_t} \right] (h - h_{ac}) - \eta \bar{V}_H C_{L_t} \\
  &= C_{m_{ac_w}} + C_L (h - h_{ac}) - \bar{V}_H C_{L_t} \impliedby \eta \approx 1 \\\\

  C_{m_0} &= C_{m_{0_w}} + C_{m_{0_t}} \\
  &= \left[ C_{m_{ac_w}} + C_{L_{0_w}} (h - h_{ac}) \right] + \left[ \eta V_H C_{L_{\alpha_t}} (\epsilon_0 + i_w - i_t) \right] \\\\

  C_{m_\alpha} &= C_{m_{\alpha_w}} + C_{m_{\alpha_t}} \\
  &= \left[ C_{L_{\alpha_w}} (h - h_{ac}) \right] + \left[ -\eta V_H C_{L_{\alpha_t}} \left( 1 - \frac{d\epsilon}{d\alpha} \right) \right] \\
  &= C_{L_\alpha} (h - h_{ac}) - \bar{V}_H \frac{d C_{L_t}}{d \alpha} \\
  &= C_{L_\alpha} (h - h_n)
\end{align*}
$$

$$
\begin{align*}
  \frac{d C_m(\alpha)}{d\alpha} &= C_{m_\alpha} = 0 \impliedby \text{Stick fixed neutrality}
\end{align*}
$$

$$
\begin{align*}
  h_n
  &= h - C_{m_\alpha} / C_{L_\alpha} \\
  &= h - dC_m / dC_L \\
  &= h_{ac} + \frac{\eta V_H C_{L_{\alpha_t}}}{C_{L_{\alpha_w}}} \left( 1 - \frac{d\epsilon}{a\alpha} \right) \impliedby \text{Stick fixed neutrality}
\end{align*}
$$

Passive stability:

$$
C_{m_\alpha} < 0 \cup C_{m_0} > 0
$$

$$
K_n = h_n - h > 0
$$

Lifts:

$$
\begin{align*}
  \epsilon
  &= 57.3 \frac{2 C_{L_w}}{\pi AR_w} &\text{for degrees} \\
  &= \epsilon_0 + \frac{d\epsilon}{d\alpha_w} \alpha_w = \frac{2 C_{L_w}}{\pi AR_w} &\text{for radians}
\end{align*}
$$

$$
\alpha_w = \alpha
$$

$$
\begin{align*}
  C_L
  &= C_{L_0} + C_{L_\alpha} \alpha \\
  &= C_{L_w} + \eta \frac{S_t}{S} C_{L_t} \\
  &= \left[ C_{L_{0_w}} - \eta \frac{S_t}{S} C_{L_{\alpha_t}} (\epsilon + i_w - i_t) \right] + \left[ C_{L_{\alpha_w}} + C_{L_{\alpha_t}} \eta \frac{S_t}{S} \right] \alpha \\
  &= \left[ C_{L_{0_w}} - \eta \frac{S_t}{S} C_{L_{\alpha_t}} (\epsilon_0 + i_w - i_t) \right] + \left[ C_{L_{\alpha_w}} + C_{L_{\alpha_t}} \eta \frac{S_t}{S} \left( 1 - \frac{d\epsilon}{d\alpha} \right) \right] \alpha
\end{align*}
$$

Elevator:

![](https://i.imgur.com/fjkXy6nm.png) ![](https://i.imgur.com/7IsJEj9m.png)

![](https://i.imgur.com/YwysCpqm.png)

$$
C_L = C_{L_\alpha} \alpha + C_{L_{\delta_e}} \delta_e
$$

$$
\alpha_{geo} = \alpha - \alpha_{0_L} \quad \text{Geometric alpha due to elevator deflection}
$$

$$
C_{L_{\delta_e}} = \left[ \eta \tau \frac{S_t}{S} \right] C_{L_{\alpha_t}}
$$

$$
C_{L_{trim}} = C_{L_\alpha} \left[ 1 - \frac{C_{L_{\delta_e}}}{C_{L_\alpha}} \frac{C_{m_\alpha}}{C_{m_{\delta_e}}} \right] \alpha_{trim} - C_{L_{\delta_e}} \frac{C_{m_0}}{C_{m_{\delta_e}}}
$$

$$
\tau = d \alpha_t / d \delta_e
$$

![](https://i.imgur.com/Wwz1NZ5m.png)

$$
\begin{align*}
  C_m &= C_{m_0} + C_{m_\alpha} \alpha + C_{m_{\delta_e}} \delta_e \\\\

  C_{mg} &= C_{m0} + \frac{d C_{m_{cg}}}{D C_L} C_L + C_{m_{\delta_e}} \delta_e \\\\

  C_{m_{\delta_e}}
  &= -\eta \tau V_H C_{L_{\alpha_t}} \\
  &= -\frac{l_t}{\bar{c}} C_{L_{\delta_e}} \\
  &= -\eta \tau \left[ \bar{V}_H - \frac{S_t}{S} (h - h_{ac}) \right] C_{L_{\alpha_t}}
\end{align*}
$$

$$
\frac{d C_{m_{cg}}}{D C_L} = \frac{X_{cg}}{\bar{c}} - \frac{X_{NP}}{\bar{c}} = h - h_{np}
$$

Rudder:

![](https://i.imgur.com/ugzKenK.png) ![](https://i.imgur.com/GngtBQYm.png)

$$
C_{n_\beta} > 0 \implies \text{Weathercock stability}
$$

$$
C_n = C_{n_\beta} \beta + C_{n_{\delta_r}} \delta_r + C_{n_{\delta_a}} \delta_a
$$

$$
C_{n_\beta} = C_{n_{\beta_{wf}}} + C_{n_{\beta_v}}
$$

$$
C_{n_{\beta_{wf}}} = -k_n k_{Rl} \frac{S_{fs} l_f}{S b} \quad \text{In /deg}
$$

$$
\begin{align*}
  S_{fs} &= \text{Projected side area of the fuselage} \\
  l_f &= \text{Total length of the fuselage (from nose tip to back of tail)} \\
  R_{l_f} &= \text{Body Reynold's number} \\
  V &= \text{Free stream velocity} \\
  v &= \text{Kinematic viscosity} \\
  x_m &= \text{Distance from the tip of the nose to the plane cg}
\end{align*}
$$

1. One begins with knowledge of $x_m / l_f$, where $x_m$ is the distance from the tip of the nose to the plane cg.
2. Knowledge of $l_f^2 / S_{fs}$ gives the first turn of the path.
3. Knowledge of the shown fuselage height ratio $\sqrt{h_1 / h_2}$ gives the second turn.
4. Knowledge of the ratio $h / w_f$ completes the third turn, allowing one to then estimate $k_n$ in the fourth and final leg.

![](https://i.imgur.com/DzvrLl2.png)

$$
R_{l_f} = V \frac{l_f}{v}
$$

![](https://i.imgur.com/6OQm8Dhm.png)

Vertical stabilizer:

$$
Y_v = - C_{L_{\alpha_v}} \alpha_v Q_v S_v \quad \text{Side force on vertical stabilizer}
$$

![](https://i.imgur.com/Rw00nfEm.png)

$$
\alpha_v = \beta + \sigma \quad \text{Sidewash angle is} ~ \sigma
$$

$$
C_{L_v} = C_{L_{\alpha_v}} \alpha_v = C_{L_{\alpha_v}} (\beta + \sigma)
$$

$$
C_{L_\beta} = C_{L_{\alpha_v}} \left( 1 + \frac{\partial \sigma}{\partial \beta} \right)
$$

$$
C_{L_{\alpha_v}} > 0 \implies \text{Passive stability}
$$

$$
N_v = -l_v Y_v = l_v C_{L_{\alpha_v}} (\beta + \sigma) Q_v S_v
$$

$$
C_{n_v} = \frac{N_v}{Q_w S b} = V_v \eta_v C_{L_{\alpha_v}} (\beta + \sigma)
$$

$$
V_v = \frac{l_v S_v}{S b}
$$

$$
\eta_n = \frac{Q_v}{Q_w}
$$

$$
\begin{align*}
  C_{n_{\beta_v}}
  &= V_v \eta_v C_{L_\beta} \\
  &= V_v \eta_v C_{L_{\alpha_v}} \left( 1 + \frac{\partial \sigma}{\partial \beta} \right)
\end{align*}
$$

$$
C_{n_{\beta_v}} > 0 \implies \text{Passive stability}
$$

$$
\eta_v \left( 1 + \frac{\partial \sigma}{\partial \beta} \right) = 0.724 + 3.06 \frac{S_v / S}{1 + \cos \Lambda_{c/4w}} + 0.4 \frac{z_w}{d} + 0.009 AR_w
$$

$$
C_{L_r} = C_{L_{\delta_r}} \delta_r
$$

$$
C_{n_r} = - V_v \eta_v C_{L_r} = -V_v \eta_v C_{L_{\delta_r}} \delta_r = C_{n_{\delta_r}} \delta_r
$$

$$
C_{n_{\delta_r}} = V_v \eta_v C_{L_{\delta_r}}
$$

$$
V_v = \frac{S_v l_v}{S b} \quad \text{Tail volume coefficient}
$$

$$
C_{L_{\delta_r}} = C_{L_{\alpha_v}} \tau = \frac{C_{L_\beta} \tau}{1 + \frac{\partial \sigma}{\partial \beta}}
$$

$$
C_{L_{\delta_r}} = \tau C_{L_{\alpha_v}} \frac{S_v}{S} \quad \text{Another equation from somewhere this time with S's idk bro}
$$

$$
C_{L_\beta} = C_{L_{\alpha_v}} \left( 1 + \frac{\partial \sigma}{\partial \beta} \right)
$$

$$
\tau = -\frac{C_{n_{\delta_r}}}{C_{n_{\beta_v}}} \left( 1 + \frac{\partial \sigma}{\partial \beta} \right)
$$

$$
\frac{\partial \sigma}{\partial \beta} = -1 - \frac{\tau C_{n_{\beta_v}}}{C_{n_{\delta_r}}}
$$

Ailerons:

![](https://i.imgur.com/67wXYZB.png)

![](https://i.imgur.com/wfaPaV2.png) ![](https://i.imgur.com/y4XtXgDm.png)

$$
C_{l_\beta} < 0 \implies \text{Passive stability}
$$

$$
\bold{V}

= V \begin{bmatrix*}
  \cos \alpha_0 \\
  \sin \alpha_0 \sin \phi \\
  \sin \alpha_0 \cos \phi
\end{bmatrix*}

= \begin{bmatrix*}
  u \\ v \\ w
\end{bmatrix*}
$$

$$
\beta = \arcsin v/V \approx = \alpha_0 \phi
$$

$$
\sin \beta = v/V = \sin \alpha_0 \sin \phi \approx \alpha_0 \phi
$$

$$
C_l = C_{l_\beta} \beta + C_{l_{\delta_r}} \delta_r + C_{l_{\delta_a}} \delta_a
$$

$$
C_{l_\beta} \beta \approx C_{l_\beta} \alpha_0 \phi
$$

$$
C_{l_\phi} \approx C_{l_\beta} \alpha_0
$$

$$
C_{l_\beta} < 0 \cup \alpha_0 > 0 \implies \text{Passive stability}
$$

$$
\Delta \alpha \approx \beta \Gamma
$$

$$
C_L = C_{L_\alpha} \alpha_a = C_{L_\alpha} \tau \delta_a
$$

Power of ailerons due to power and position:

![](https://i.imgur.com/yYjXN31.png)

$$
C_{l_{\delta_a}} = \frac{2 C_{L_{\alpha_w}} \tau}{S b} \int_{y_1}^{y_2} c y dy
$$

$$
c = c_r \left[ 1 + \frac{\lambda - 1}{b / 2} y \right]
$$

$$
\lambda = \frac{c_t}{c_r}
$$

$$
\Delta_a = y_2 - y_1
$$

$$
\mu_a = \frac{y_1 + y_2}{2}
$$

$$
\begin{align*}
  C_{l_{\delta_a}}
  &= \frac{2 C_{L_{\alpha_w}} \tau c_r \Delta_a}{S b} \left[ \mu_a - \frac{1 - \lambda}{b} (2 \mu_a^2 + \Delta_a^2/6) \right] \\
  &\approx \frac{2 C_{L_{\alpha_w}} \tau c_r \Delta_a \mu_a}{S b} \left[ 1 - \frac{1 - \lambda}{b/2} \mu_a \right]
\end{align*}
$$

Optimum aileron position:

$$
\mu_a^* = \frac{b / 2}{2 (1 - \lambda)}
$$

## Primitive Motion

Euler's equations of motion (rad):

$$
L = I_x \dot{p} - I_{zx} \dot{r} + q r (I_z - I_y) - I_{zx} p q
$$

$$
M = I_y \dot{q} + r p (I_x - I_z) + I_{zx} (p^2 - r^2)
$$

$$
N = I_z \dot{r} - I_{zx} \dot{p} + p q (I_y - I_x) - I_{zx} q r
$$

Matrix form:

$$
L_1(X_1) = \begin{bmatrix}
  1 & 0 & 0 \\
  0 & \cos X_1 & \sin X_1 \\
  0 & -\sin X_1 & \cos X_1
\end{bmatrix}
$$

$$
L_2(X_2) = \begin{bmatrix}
  \cos X_2 & 0 & -\sin X_2 \\
  0 & 1 & 0 \\
  \sin X_2 & 0 & \cos X_2
\end{bmatrix}
$$

$$
L_3(X_3) = \begin{bmatrix}
  \cos X_3 & \sin X_3 & 0 \\
  -\sin X_3 & \cos X_3 & 0 \\
  0 & 0 & 1
\end{bmatrix}
$$

![](https://i.imgur.com/XT2cBdWl.png)

$\psi$ is the rotation about $z_f$ called yaw/azimuth

$\theta$ is the rotation about $y_1$ called elevation

$\phi$ is the rotation about $x_2$ called roll

Rotation matrix:

$$
\begin{bmatrix}
  x_1 \\
  y_1 \\
  z_1
\end{bmatrix} = \begin{bmatrix}
  C_\psi & S_\psi & 0 \\
  -S_\psi & C_\psi & 0 \\
  0 & 0 & 1
\end{bmatrix} \begin{bmatrix}
  x_f \\
  y_f \\
  z_f
\end{bmatrix} = R_{z_f} \begin{bmatrix}
  x_f \\
  y_f \\
  z_f
\end{bmatrix}
$$

$$
\begin{bmatrix}
  x_2 \\
  y_2 \\
  z_2
\end{bmatrix} = R_{y_1} (\theta) \begin{bmatrix}
  x_1 \\
  y_1 \\
  z_1
\end{bmatrix}
$$

$$
\begin{bmatrix}
  x_b \\
  y_b \\
  z_b
\end{bmatrix} = R_{x_2} (\phi) \begin{bmatrix}
  x_2 \\
  y_2 \\
  z_2
\end{bmatrix}
$$

All combined:

$$
L_{BE} = \begin{bmatrix}
  \cos \theta \cos \psi & \cos \theta \sin \psi & -\sin \theta \\
  \sin \phi \sin \theta \cos \psi - \cos \phi \sin \psi & \sin \phi \sin \theta \sin \psi + \cos \phi \cos \psi & \sin \phi \cos \theta \\
  \cos \phi \sin \theta \cos \psi + \sin \phi \sin \psi & \cos \phi \sin \theta \sin \psi - \sin \phi \cos \psi & \cos \phi \cos \theta
\end{bmatrix}
$$

Body forces:

$$
F_{x_{gr}} = -m g \sin \theta
$$

$$
F_{y_{gr}} = m g \cos \theta \cos \phi
$$

$$
F_{z_{gr}} = m g \cos \theta \cos \phi
$$

## Kinematic Summary

Forces:

$$
X - m g \sin \theta = m(\dot{u} + q w - r v)
$$

$$
Y + m g \cos \theta \cos \phi = m(\dot{v} + r u - p w)
$$

$$
Z + m g \cos \theta \cos \phi = m(\dot{w} + p v - q u)
$$

Moments:

$$
L = I_x \dot{p} - I_{xz} \dot{r} + q r (I_z - I_x) - I_{xz} p q
$$

$$
M = I_y \dot{q} + r p (I_x - I_z) - I_{xz} (p^2 - r^2)
$$

$$
N = I_z \dot{r} - I_{xz} \dot{p} + p q (I_y - I_x) + I_{xz} q r
$$

Angular rates:

$$
\begin{bmatrix}
  p \\
  q \\
  r
\end{bmatrix} = \begin{bmatrix}
  1 & 0 & -S_\theta \\
  0 & C_\phi & C_\theta S_\phi \\
  0 & -S_\phi & C_\theta C_\phi
\end{bmatrix} \begin{bmatrix}
  \dot{\phi} \\
  \dot{\theta} \\
  \dot{\psi}
\end{bmatrix}
$$

Euler rates:

$$
\begin{bmatrix}
  \dot{\phi} \\
  \dot{\theta} \\
  \dot{\psi}
\end{bmatrix} = \begin{bmatrix}
  1 & S_\phi \tan \theta & C_\phi \tan \theta \\
  0 & C_\phi & -S_\phi \\
  0 & S_\phi \sec \theta & C_\phi \sec \theta
\end{bmatrix} \begin{bmatrix}
  p \\
  q \\
  r
\end{bmatrix}
$$

Fixed frame velocities:

$$
\begin{bmatrix}
  \dot{x} \\
  \dot{y} \\
  \dot{z}
\end{bmatrix} = \begin{bmatrix}
  C_\theta C_\psi & S_\phi S_\theta C_\psi - C_\phi S_\psi & C_\phi S_\theta C_\psi + S_\phi S_\psi \\
  C_\theta S_\psi & S_\phi S_\theta S_\psi + C_\phi C_\psi & C_\phi S_\theta S_\psi - S_\phi C_\psi \\
  -S_\theta & S_\phi C_\theta & C_\phi C_\theta
\end{bmatrix} \begin{bmatrix}
  u \\
  v \\
  w
\end{bmatrix}
$$

Small perturbation:

$$
\begin{bmatrix}
  \Delta \dot{\phi} \\
  \Delta \dot{\theta} \\
  \Delta \dot{\psi}
\end{bmatrix} = \begin{bmatrix}
  1 & S_{\Delta \phi} \tan(\theta_0 + \Delta \theta) & C_{\Delta \phi} \tan(\theta_0 + \Delta \theta) \\
  0 & C_{\Delta \phi} & -S_{\Delta \phi} \\
  0 & S_{\Delta \phi} \sec(\theta_0 + \Delta \theta) & C_{\Delta \phi} \sec(\theta_0 + \Delta \theta)
\end{bmatrix} \begin{bmatrix}
  \Delta p \\
  \Delta q \\
  \Delta r
\end{bmatrix}
$$

Extracted and simplified equations from matrix above:

$$
\Delta \dot{\phi} \approx \Delta p + \Delta r \tan \theta_0
$$

$$
\Delta \dot{\theta} \approx \Delta q
$$

$$
\Delta \dot{\psi} \approx \Delta r \sec \theta_0
$$

Reduced matrix:

$$
\begin{bmatrix}
  \dot{\phi} \\
  \Delta \dot{\theta} \\
  \dot{\psi}
\end{bmatrix} \approx \begin{bmatrix}
  1 & 0 & \tan \theta_0 \\
  0 & 1 & 0 \\
  0 & 0 & \sec \theta_0
\end{bmatrix} \begin{bmatrix}
  p \\
  q \\
  r
\end{bmatrix}
$$

Reference condition:

$$
\omega_0 = \begin{bmatrix}
  p_0 & q_0 & r_0
\end{bmatrix}^T = 0
$$

$$
V_0 = \begin{bmatrix}
  u_0 & v_0 & w_0
\end{bmatrix}^T = \begin{bmatrix}
  u_0 & 0 & 0
\end{bmatrix}^T
$$

$$
\begin{bmatrix}
  \phi_0 & \theta_0 & \psi_0
\end{bmatrix} = \begin{bmatrix}
  0 & \theta_0 & 0
\end{bmatrix}
$$

Longitudinal small-disturbance:

$$
\left( \frac{d}{dt} - X_u \right) \Delta u - X_w \Delta w + (g \cos \theta_0) \Delta \theta = X_{\delta_e} \Delta \delta_e + X_{\delta_T} \Delta \delta_T
$$

$$
-Z_u \Delta u + \left[ (1 - Z_{\dot{w}}) \frac{d}{dt} - Z_w \right] \Delta w - \left[ (u_0 + Z_q) \frac{d}{dt} - g \sin \theta_0 \right] \Delta \theta = Z_{\delta_e} \Delta \delta_e + Z_{\delta_T} \Delta \delta_T
$$

$$
-M_u \Delta u - \left( M_{\dot{w}} \frac{d}{dt} + M_w \right) \Delta w - \left( \frac{d^2}{dt^2} - M_q \frac{d}{dt} \right) \Delta \theta = M_{\delta_e} \Delta \delta_e + M_{\delta_T} \Delta \delta_T
$$

Lateral small-disturbance:

$$
\left( \frac{d}{dt} - Y_v \right) \Delta v - Y_p \Delta p + (u_0 - Y_r) \Delta r - (g \cos \theta_0) \Delta \phi = Y_{\delta_r} \Delta \delta_r
$$

$$
-L_v \Delta v + \left( \frac{d}{dt} - L_p \right) \Delta p - \left( \frac{I_{yz}}{I_x} \frac{d}{dt} + L_r \right) \Delta r = L_{\delta_a} \Delta \delta_a + L_{\delta_r} \Delta \delta_r
$$

$$
-N_v \Delta v - \left( \frac{I_{xz}}{I_z} \frac{d}{dt} + N_p \right) \Delta p + \left( \frac{d}{dt} - N_r \right) \Delta r = N_{\delta_a} \Delta \delta_a + N_{\delta_r} \Delta \delta_r
$$

With simplification and conversion to matrix form:

$$
\begin{bmatrix}
  \Delta \dot{u} \\
  \dot{w} \\
  \dot{q} \\
  \Delta \dot{\theta}
\end{bmatrix} = \begin{bmatrix}
  X_u & X_w & 0 & -g \cos \theta_0 \\
  \frac{Z_u}{1 - Z_{\dot{w}}} & \frac{Z_w}{1 - Z_{\dot{w}}} & \frac{u_0 + Z_q}{1 - Z_{\dot{w}}} & \frac{-g \sin \theta_0}{1 - Z_{\dot{w}}} \\
  M_u + \frac{M_{\dot{w}} Z_u}{1 - Z_{\dot{w}}} & M_w + \frac{M_{\dot{w}} Z_w}{1 - Z_{\dot{w}}} & M_q + \frac{M_{\dot{w}} (u_0 + Z_q)}{1 - Z_{\dot{w}}} & \frac{-M_{\dot{w}} g \sin \theta_0}{1 - Z_{\dot{w}}} \\
  0 & 0 & 1 & 0
\end{bmatrix} \begin{bmatrix}
  \Delta u \\
  w \\
  q \\
  \Delta \theta
\end{bmatrix} + \begin{bmatrix}
  X_{\delta_e} & X_{\delta_T} \\
  \frac{Z_{\delta_e}}{1 - Z_{\dot{w}}} & \frac{Z_{\delta_T}}{1 - Z_{\dot{w}}} \\
  M_{\delta_e} + \frac{M_{\dot{w}} Z_{\delta_e}}{1 - Z_{\dot{w}}} & M_{\delta_T} + \frac{M_{\dot{w}} Z_{\delta_T}}{1 - Z_{\dot{w}}} \\
  0 & 0
\end{bmatrix} \begin{bmatrix}
  \Delta \delta_e \\
  \Delta \delta_T
\end{bmatrix}
$$

All derivatives:

![](https://i.imgur.com/b2VjOYyl.png) ![](https://i.imgur.com/DZYCjRrl.png)

Stability equations:

![](https://i.imgur.com/3BXbr4X.png)

## Longitudinal Motion

Level flight simplification:

![](https://i.imgur.com/xCI6PhX.png)

General form of a first order system response $y(t)$ to initial condition $y_0$ and step input $f(t) = F_0$:

$$
y(t) = y_0 e^{-t / \tau} + g_s F_0 (1 - e^{-t / \tau})
$$

$$
\tau \dot{y} + y = g_s f(t)
$$

$$
p(s) = s + \frac{1}{\tau}
$$

Doubling and halving time of first order system:

$$
t_\text{doubling} = \frac{\ln 2}{s}
$$

$$
t_\text{halving} = -\frac{\ln 2}{s}
$$

Second order dynamics:

$$
m \ddot{x} + c \dot{x} + k x = 0
$$

$$
\ddot{x} + 2 \zeta \omega_n \dot{x} + \omega_n^2 x = 0
$$

$$
\omega_n = \sqrt{\frac{k}{m}} = 2 \pi f_n \quad \omega_d = \omega_n \sqrt{1 - \zeta^2}
$$

$$
T = 2\pi / \omega_n \quad T_d = 2 \pi / \omega_d \quad \tau = \frac{1}{\zeta \omega_n}
$$

$$
\zeta = \frac{c}{2 m \omega_n}
$$

Function of time:

$$
x(t) = C e^{-\zeta \omega_n t} \cos \left( \omega_d t + \psi \right)
$$

$$
\dot{x}(t) = -C e^{-\zeta \omega_n t} (\zeta \omega_n \cos(\omega_d t + \psi) + \omega_d \sin(\omega_d t + \psi))
$$

Characteristic equation:

$$
p(s) = s^2 + 2 \zeta \omega_n s + \omega_n^2 = 0
$$

$$
s_{1,2} = -\zeta \pm i \omega_n \sqrt{1 - \zeta^2} = -\zeta \pm i \omega_d
$$

Euler, my brotha' for another mother:

$$
e^{i \phi} = \cos \phi + i \sin \phi
$$

![](https://i.imgur.com/fVVnkdjl.png)

## State Space

Transformation example because I am an idiot and always forget how to do this:

$$
\ddot{\theta} + 2 \dot{\theta} + 5 \theta = -\delta
$$

$$
x = \theta
$$

$$
\ddot{x} + 2 \dot{x} + 5 x = -\delta
$$

$$
x_1 = x
$$

$$
x_2 = \dot{x}_1 = \dot{x}
$$

$$
\ddot{x} = -\delta - 2 \dot{x} - 5 x
$$

$$
X = \begin{bmatrix}
  x_1 \\
  x_2
\end{bmatrix}
$$

$$
\dot{X} = \begin{bmatrix}
  x_2 \\
  -\delta - 2 x_2 - 5 x_1
\end{bmatrix}
$$

$$
\boxed{\dot{X} = \begin{bmatrix}
  0 & 1 \\
  -5 & -2
\end{bmatrix} X + \begin{bmatrix}
  0 \\
  -1
\end{bmatrix}
\delta}
$$

## Pitching

Toy example:

![](https://i.imgur.com/ErKeyftm.png)

$$
\ddot{\theta} - \frac{l}{u_0} M_\alpha \dot{\theta} - M_\alpha \theta = 0
$$

$$
\omega_n = \sqrt{-M_\alpha}
$$

Step elevator deflection:

$$
\Delta \alpha (t) = \Delta \alpha_{trim} \left( 1 + \frac{e^{-\zeta \omega_n t}}{\sqrt{1 - \zeta^2}} \sin (\omega_d t + \phi) \right)
$$

$$
\phi = \arctan \frac{\sqrt{1 - \zeta^2}}{\zeta}
$$

$$
\Delta \alpha_{trim} = -\frac{M_{\delta_e}}{M_\alpha} \Delta \delta_e
$$

Phugoid (long) approximation:

![](https://i.imgur.com/ePANt9y.png)

$$
\begin{bmatrix}
  \Delta \dot{u} \\
  \Delta \dot{\theta}
\end{bmatrix} = \begin{bmatrix}
  X_u & -g \\
  -Z_u / u_0 & 0
\end{bmatrix} \begin{bmatrix}
  \Delta u \\
  \Delta \theta
\end{bmatrix}
$$

$$
s_{1,2} = \frac{1}{2} \left( X_u \pm \sqrt{X_u^2 + \frac{4 g Z_u}{u_0}} \right)
$$

$$
\zeta_p = \frac{-X_u}{2 \omega_{n_p}}
$$

$$
\omega_{n_p} = \sqrt{\frac{-g Z_u}{u_0}}
$$

And this is important when finding $Z_u$:

$$
C_{L_u} = \frac{M^2}{1 - M^2} C_{L_0}
$$

Short period approximation:

$$
\begin{bmatrix}
  \Delta \dot{w} \\
  \Delta \dot{q}
\end{bmatrix} = \begin{bmatrix}
  Z_w & u_0 \\
  M_w + M_{\dot{w}} Z_w & M_{\dot{w}} u_0 + M_q
\end{bmatrix} \begin{bmatrix}
  \Delta w \\
  \Delta q
\end{bmatrix}
$$

$$
\begin{bmatrix}
  \Delta \dot{\alpha} \\
  \Delta \dot{q}
\end{bmatrix} = \begin{bmatrix}
  Z_w & 1 \\
  (M_w + M_{\dot{w}} Z_w) u_0 & M_{\dot{w}} u_0 + M_q
\end{bmatrix} \begin{bmatrix}
  \Delta \alpha \\
  \Delta q
\end{bmatrix}
$$

Same assumptions as the short period approximation but for the full model:

THIS IS NOT THE FULL MODEL. FUTURE ME, GO BACK A FEW PAGES FOR THE FULL MODEL.

![](https://i.imgur.com/Ckw43RB.png)

That leads to:

![](https://i.imgur.com/WtfwU26m.png)

$$
p(s) = s^2 - \left( M_q + M_{\dot{\alpha}} + \frac{Z_\alpha}{u_0} \right) s + \left( M_q \frac{Z_\alpha}{u_0} - M_\alpha \right) = 0
$$

Nap time... I mean... summary time:

![](https://i.imgur.com/C9IsWMW.png)

## Lateral Motion

![](https://i.imgur.com/0WhDo4a.png)

Basic transfer function:

$$
\tau s Y(s) + Y(s) = g_s F(s)
$$

$$
G(s) = \frac{\text{output}}{\text{input}} = \frac{Y(s)}{F(s)} = \frac{g_s}{\tau s + 1}
$$

$$
Y(s) = G(s) F(s) = \frac{g_s}{\tau s + 1} F(s)
$$

Laplace transforms:

![](https://i.imgur.com/C3EqOv9m.png) ![](https://i.imgur.com/JVzLMm9m.png)

System unit impulse response:

$$
Y(S) = G(s) 1 = \frac{g_s}{\tau s + 1}
$$

$$
y(t) = \frac{g_s}{\tau} e^{-t / \tau}
$$

System unit step response:

$$
Y(s) = G(s) \frac{1}{s} = g_s \frac{1 / \tau}{s (s + 1 / \tau)}
$$

$$
y(t) = g_s (1 - e^{-t / \tau})
$$

$$
y_{ss} = \lim_{t \to \infty} y(t) = g_s
$$

Roll differential equation:

$$
I_x \Delta \dot{p} - \frac{\partial L}{\partial p} \Delta p = \frac{\partial L}{\partial \delta_a} \Delta \delta_a
$$

$$
\tau = I_x \left( \frac{-\partial L}{\partial p} \right)^{-1}
$$

$$
g_s = \frac{\partial L}{\partial \delta_a} \left( \frac{-\partial L}{\partial p} \right)^{-1}
$$

$\tau > 0 \implies$ stability. In other words, $\partial L / \partial p < 0$

$4 \tau$ response time is proportional to $I_x$ and inversely to $|\partial L / \partial p|$

$[\Delta \delta_a] = rad$, $\Delta p(t) = rad/s$, $[g_s] = 1/s$

Static gain $g_s$ is proportional to $\partial L / \partial \delta_a$ and inversely to $|\partial L / \partial p|$

$$
\frac{-1}{L_p} \Delta \dot{p} + \Delta p = \frac{-L_{\delta_a}}{L_p} \Delta \delta_a
$$

$$
\tau = \frac{-1}{L_p} \quad g_s = \frac{-L_{\delta_a}}{L_p}
$$

Yawing moment:

![](https://i.imgur.com/mptewiq.png)

$$
\ddot{\psi} + (N_{\dot{\beta}} - N_r) \dot{\psi} + N_\beta \psi = N_{\delta_r} \delta_r
$$

$$
N_{\dot{\beta}} \approx 0
$$

$$
\omega_n = \sqrt{N_\beta} \quad \zeta = - \frac{N_r}{2 \sqrt{N_\beta}} \quad \tau = -2/N_r \quad g_s = N_{\delta_r} / N_\beta
$$

$$
\omega_d = \frac{1}{2} \sqrt{4 N_\beta - N_r^2} \quad T_d = \frac{4 \pi}{\sqrt{4 N_\beta - N_r^2}}
$$

![](https://i.imgur.com/1CD1eOqm.png)

$$
\beta_{ss} = \lim_{t \to \infty} \beta(t) = -g_s \delta_r = \beta_{TRIM}
$$

Matrix form:

![](https://i.imgur.com/E1bXNt8m.png)

Here, ss means steady state, or when the state no longer changes.

$$
\dot{x} = 0 = A x + B u
$$

So just solve for $x$ to get the steady state:

$$
x_{ss} = -A^{-1} B u
$$

Get $\phi_{ss}$ like that too:

$$
\phi_{ss} = x_{ss}(2)
$$

![](https://i.imgur.com/iXtvqsll.png)

Pure roll dynamics:

$$
\tau \Delta \dot{p} + p = g_s \Delta \delta_a
$$

$$
\tau = \frac{-1}{L_p} \quad g_s = -\frac{L_{\delta_a}}{L_p}
$$

## Lateral Directional Motion

![](https://i.imgur.com/sK4KGukl.png)

![](https://i.imgur.com/J2hhSKdl.png)

Spiral more approximation:

![](https://i.imgur.com/tlegaVpm.png)

$$
\Delta \dot{r} + \frac{L_r N_\beta - L_\beta N_r}{L_\beta} \Delta r = 0
$$

$$
s_1 = - \frac{L_r N_\beta - L_\beta N_r}{L_\beta}
$$

$$
\tau = \frac{L_\beta}{L_r N_\beta - L_\beta N_r}
$$

$L_\beta N_r > L_r N_\beta$ required for stability.

Rolling mode approximation:

$$
\Delta \dot{p} - L_p \Delta p = 0
$$

$$
s_1 = L_p
$$

$$
\tau = \frac{-1}{L_p}
$$

Dutch roll approximation:

![](https://i.imgur.com/zx4L9b3m.png)

![](https://i.imgur.com/gCPaaOwm.png) ![](https://i.imgur.com/76MfuWcm.png)

Lol I always forget the quadratic formula:

$$
ax^2 + bx + c = 0 \implies  x = \frac{-b \pm \sqrt{b^2 - 4 a c}}{2 a}
$$

![](https://i.imgur.com/LXQzNzEm.png) ![](https://i.imgur.com/dq2p5qMm.png)

Identifying roots example:

![](https://i.imgur.com/YWWEU7Hm.png)

The left most root is the roll mode:

$$
s_1 = \boxed{-7 rad/s}
$$

The one with the imaginary parts is the dutch roll:

$$
s_{2,3} = \boxed{(-2 \pm 3i) rad/s}
$$

And the one close to the origin, or the one to the right is the spiral mode:

$$
s_4 = \boxed{-0.5 rad/s}
$$

Interpreting eigenvalues:

$$
\sigma = \real(\lambda) \quad \omega_d = \image(\lambda)
$$

$$
\omega_n = \sqrt{\sigma^2 + \omega_d^2} \quad \zeta = -\sigma / \omega_n
$$

Or poles:

$$
s_1, s_2 = -\sigma \pm i \omega_d
$$

$$
s^2 + 2 \zeta \omega_n s + \omega_n^2 = 0
$$

Relevant derivatives:

![](https://i.imgur.com/d6D8sIX.png)

$$
C_{y_r} \approx -C_{n_r} \frac{b}{l_v}
$$

$$
Y_r \neq 0 \quad \frac{Y_r}{u_0} \approx 0
$$

Lateral directional motion:

![](https://i.imgur.com/kTWXB41.png)

However, with $I_{xz} \approx 0$, we can simplify:

![](https://i.imgur.com/n4CgoxV.png)

And with $\Delta \beta \approx \arctan \frac{\Delta v}{u_0} \approx \frac{\Delta v}{u_0}$:

![](https://i.imgur.com/EmAHedC.png)

## Control Theory

![](https://i.imgur.com/fH6Fkftl.png)

System is stable if and only if all roots are in the left half plane.

Second order system with damping:

![](https://i.imgur.com/KdrzjUGm.png)

Roots of the numerator are called the zeros while the ones for the denominates are the poles. Poles:

![](https://i.imgur.com/qXdD0qbm.png)

Characteristic equation for second order system:

![](https://i.imgur.com/NnTvZ09m.png)

$c < 2 \sqrt{k m} \implies$ underdamped

$c > 2 \sqrt{k m} \implies$ overdamped. $p_{1,2} = 1 / \tau_{1,2}$ and $\zeta > 1$

$c = 2 \sqrt{k m} \implies$ critically damped. $p_{1,2} = 1 / \tau$ and $\zeta = 1$

Toy system that MATLAB `rlocus` assumes:

![](https://i.imgur.com/KweVQkYm.png)

$$
\frac{Y}{U} = \frac{G(s)}{1 + K G(s)} = W(s)
$$

$$
p(s) = m s^2 + c s + k = 0
$$

Making $m$ the tuning gain:

$$
G(s) = \frac{s^2}{c s + k}
$$
