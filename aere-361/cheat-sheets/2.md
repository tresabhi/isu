# AERE 361 Cheat Sheet 1

# GCC

- `-std=c99 -pedantic -Wall -Werror` boosts warning verbosity
- `-g` enables gdb diagnostics

## GDB

- `gdb <FILENAME>` spawns GDB
- `gdb -args <FILENAME> <FLAGS>` lets you pass flags
- `break <FUNCTION NAME>`, `break <LOC>`, `break <FILENAME>:<LOC>` create breakpoints
- `info break` lists all break points
- `delete <INDEX>` deletes a breakpoint
- `run` runs the code
- `next` executes the next line (executes whole functions)
- `continue` unpauses from a breakpoint
  - `step` can jump into any function it runs into
- `print <SYMBOL>` prints the value of the symbol
  - `watch <SYMBOL>` automates this on change
